# OpenRouter API key — get one at https://openrouter.ai/keys
# Used to authenticate requests to OpenRouter's model endpoints.
# Use if extending the example to use an LLM.
OPENROUTER_API_KEY='YOUR API KEY HERE'
